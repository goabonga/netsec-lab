# netlab-icmp

Hijack routing via ICMP redirect; covert ICMP channel.

Layer **L3** - part of [netsec-lab](../../README.md).

```bash
netlab-icmp brief     # what this module teaches
netlab-icmp attack --i-own-this-network   # lab only
netlab-icmp detect
netlab-icmp defend
```

Full lesson: see the [documentation](https://goabonga.github.io/netsec-lab/layer-3-network/icmp/).
