# netlab-netflow

Generate and analyze flows for behavioural detection.

Layer **Tooling** - part of [netsec-lab](../../README.md).

```bash
netlab-netflow brief     # what this module teaches
netlab-netflow attack --i-own-this-network   # lab only
netlab-netflow detect
netlab-netflow defend
```

Full lesson: see the [documentation](https://goabonga.github.io/netsec-lab/defensive-tooling/netflow/).
