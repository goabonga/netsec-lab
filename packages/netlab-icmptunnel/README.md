# netlab-icmptunnel

Exfiltrate in ICMP echo payloads (companion to the DNS tunnel).

Layer **Services** - part of [netsec-lab](../../README.md).

```bash
netlab-icmptunnel brief     # what this module teaches
netlab-icmptunnel attack --i-own-this-network   # lab only
netlab-icmptunnel detect
netlab-icmptunnel defend
```

Full lesson: see the [documentation](network-services/icmp-tunneling.md).
