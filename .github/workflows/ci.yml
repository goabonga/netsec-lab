# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Chris <goabonga@pm.me>

# GENERATED by scripts/gen_ci.py from scripts/catalogue.py - do not edit by hand.
#
# No matrix: one explicit job per package per check, each gated on whether that
# component actually changed (multicz changed). The release fan-out bumps the
# workspace and creates a GitHub release per bumped package (+ the docs site).

name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:

  detect:
    name: detect changed components
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.changed.outputs.changed }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Validate multicz configuration
        run: uv tool run multicz validate --strict
      - id: changed
        name: Compute changed components
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            CH=$(uv tool run multicz changed --since "origin/${{ github.base_ref }}" --output json | jq -c '.changed')
          else
            CH=$(uv tool run multicz changed --output json | jq -c '.changed')
          fi
          echo "changed=$CH" >> "$GITHUB_OUTPUT"
          echo "## Changed: \`$CH\`" >> "$GITHUB_STEP_SUMMARY"

  headers:
    name: license-headers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: python3 scripts/add_license_header.py --path packages --types py --check
      - run: python3 scripts/add_license_header.py --path scripts --types py --check

  netlab-core-lint:
    name: lint (netlab-core)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-core')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-core
          module: netlab_core
          check: lint

  netlab-core-typecheck:
    name: typecheck (netlab-core)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-core')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-core
          module: netlab_core
          check: typecheck

  netlab-core-test:
    name: test (netlab-core)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-core')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-core
          module: netlab_core
          check: test

  netlab-wifi-recon-lint:
    name: lint (netlab-wifi-recon)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-recon
          module: netlab_wifi_recon
          check: lint

  netlab-wifi-recon-typecheck:
    name: typecheck (netlab-wifi-recon)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-recon
          module: netlab_wifi_recon
          check: typecheck

  netlab-wifi-recon-test:
    name: test (netlab-wifi-recon)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-recon
          module: netlab_wifi_recon
          check: test

  netlab-wifi-deauth-lint:
    name: lint (netlab-wifi-deauth)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-deauth')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-deauth
          module: netlab_wifi_deauth
          check: lint

  netlab-wifi-deauth-typecheck:
    name: typecheck (netlab-wifi-deauth)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-deauth')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-deauth
          module: netlab_wifi_deauth
          check: typecheck

  netlab-wifi-deauth-test:
    name: test (netlab-wifi-deauth)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-deauth')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-deauth
          module: netlab_wifi_deauth
          check: test

  netlab-evil-twin-lint:
    name: lint (netlab-evil-twin)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-evil-twin')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-evil-twin
          module: netlab_evil_twin
          check: lint

  netlab-evil-twin-typecheck:
    name: typecheck (netlab-evil-twin)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-evil-twin')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-evil-twin
          module: netlab_evil_twin
          check: typecheck

  netlab-evil-twin-test:
    name: test (netlab-evil-twin)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-evil-twin')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-evil-twin
          module: netlab_evil_twin
          check: test

  netlab-wpa-crack-lint:
    name: lint (netlab-wpa-crack)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wpa-crack')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wpa-crack
          module: netlab_wpa_crack
          check: lint

  netlab-wpa-crack-typecheck:
    name: typecheck (netlab-wpa-crack)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wpa-crack')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wpa-crack
          module: netlab_wpa_crack
          check: typecheck

  netlab-wpa-crack-test:
    name: test (netlab-wpa-crack)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wpa-crack')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wpa-crack
          module: netlab_wpa_crack
          check: test

  netlab-wps-brute-lint:
    name: lint (netlab-wps-brute)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wps-brute')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wps-brute
          module: netlab_wps_brute
          check: lint

  netlab-wps-brute-typecheck:
    name: typecheck (netlab-wps-brute)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wps-brute')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wps-brute
          module: netlab_wps_brute
          check: typecheck

  netlab-wps-brute-test:
    name: test (netlab-wps-brute)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wps-brute')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wps-brute
          module: netlab_wps_brute
          check: test

  netlab-tap-lint:
    name: lint (netlab-tap)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tap')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tap
          module: netlab_tap
          check: lint

  netlab-tap-typecheck:
    name: typecheck (netlab-tap)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tap')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tap
          module: netlab_tap
          check: typecheck

  netlab-tap-test:
    name: test (netlab-tap)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tap')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tap
          module: netlab_tap
          check: test

  netlab-tempest-lint:
    name: lint (netlab-tempest)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tempest')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tempest
          module: netlab_tempest
          check: lint

  netlab-tempest-typecheck:
    name: typecheck (netlab-tempest)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tempest')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tempest
          module: netlab_tempest
          check: typecheck

  netlab-tempest-test:
    name: test (netlab-tempest)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tempest')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tempest
          module: netlab_tempest
          check: test

  netlab-hw-implant-lint:
    name: lint (netlab-hw-implant)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-hw-implant')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-hw-implant
          module: netlab_hw_implant
          check: lint

  netlab-hw-implant-typecheck:
    name: typecheck (netlab-hw-implant)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-hw-implant')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-hw-implant
          module: netlab_hw_implant
          check: typecheck

  netlab-hw-implant-test:
    name: test (netlab-hw-implant)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-hw-implant')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-hw-implant
          module: netlab_hw_implant
          check: test

  netlab-dhcp-lint:
    name: lint (netlab-dhcp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dhcp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dhcp
          module: netlab_dhcp
          check: lint

  netlab-dhcp-typecheck:
    name: typecheck (netlab-dhcp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dhcp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dhcp
          module: netlab_dhcp
          check: typecheck

  netlab-dhcp-test:
    name: test (netlab-dhcp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dhcp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dhcp
          module: netlab_dhcp
          check: test

  netlab-arp-lint:
    name: lint (netlab-arp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-arp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-arp
          module: netlab_arp
          check: lint

  netlab-arp-typecheck:
    name: typecheck (netlab-arp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-arp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-arp
          module: netlab_arp
          check: typecheck

  netlab-arp-test:
    name: test (netlab-arp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-arp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-arp
          module: netlab_arp
          check: test

  netlab-macflood-lint:
    name: lint (netlab-macflood)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-macflood')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-macflood
          module: netlab_macflood
          check: lint

  netlab-macflood-typecheck:
    name: typecheck (netlab-macflood)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-macflood')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-macflood
          module: netlab_macflood
          check: typecheck

  netlab-macflood-test:
    name: test (netlab-macflood)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-macflood')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-macflood
          module: netlab_macflood
          check: test

  netlab-stp-lint:
    name: lint (netlab-stp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-stp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-stp
          module: netlab_stp
          check: lint

  netlab-stp-typecheck:
    name: typecheck (netlab-stp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-stp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-stp
          module: netlab_stp
          check: typecheck

  netlab-stp-test:
    name: test (netlab-stp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-stp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-stp
          module: netlab_stp
          check: test

  netlab-vlan-lint:
    name: lint (netlab-vlan)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-vlan')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-vlan
          module: netlab_vlan
          check: lint

  netlab-vlan-typecheck:
    name: typecheck (netlab-vlan)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-vlan')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-vlan
          module: netlab_vlan
          check: typecheck

  netlab-vlan-test:
    name: test (netlab-vlan)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-vlan')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-vlan
          module: netlab_vlan
          check: test

  netlab-ipv6-ra-lint:
    name: lint (netlab-ipv6-ra)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ipv6-ra')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ipv6-ra
          module: netlab_ipv6_ra
          check: lint

  netlab-ipv6-ra-typecheck:
    name: typecheck (netlab-ipv6-ra)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ipv6-ra')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ipv6-ra
          module: netlab_ipv6_ra
          check: typecheck

  netlab-ipv6-ra-test:
    name: test (netlab-ipv6-ra)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ipv6-ra')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ipv6-ra
          module: netlab_ipv6_ra
          check: test

  netlab-discovery-lint:
    name: lint (netlab-discovery)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-discovery')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-discovery
          module: netlab_discovery
          check: lint

  netlab-discovery-typecheck:
    name: typecheck (netlab-discovery)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-discovery')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-discovery
          module: netlab_discovery
          check: typecheck

  netlab-discovery-test:
    name: test (netlab-discovery)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-discovery')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-discovery
          module: netlab_discovery
          check: test

  netlab-lldp-med-lint:
    name: lint (netlab-lldp-med)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-lldp-med')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-lldp-med
          module: netlab_lldp_med
          check: lint

  netlab-lldp-med-typecheck:
    name: typecheck (netlab-lldp-med)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-lldp-med')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-lldp-med
          module: netlab_lldp_med
          check: typecheck

  netlab-lldp-med-test:
    name: test (netlab-lldp-med)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-lldp-med')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-lldp-med
          module: netlab_lldp_med
          check: test

  netlab-8021x-lint:
    name: lint (netlab-8021x)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-8021x')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-8021x
          module: netlab_8021x
          check: lint

  netlab-8021x-typecheck:
    name: typecheck (netlab-8021x)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-8021x')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-8021x
          module: netlab_8021x
          check: typecheck

  netlab-8021x-test:
    name: test (netlab-8021x)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-8021x')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-8021x
          module: netlab_8021x
          check: test

  netlab-macsec-lint:
    name: lint (netlab-macsec)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-macsec')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-macsec
          module: netlab_macsec
          check: lint

  netlab-macsec-typecheck:
    name: typecheck (netlab-macsec)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-macsec')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-macsec
          module: netlab_macsec
          check: typecheck

  netlab-macsec-test:
    name: test (netlab-macsec)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-macsec')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-macsec
          module: netlab_macsec
          check: test

  netlab-ipspoof-lint:
    name: lint (netlab-ipspoof)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ipspoof')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ipspoof
          module: netlab_ipspoof
          check: lint

  netlab-ipspoof-typecheck:
    name: typecheck (netlab-ipspoof)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ipspoof')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ipspoof
          module: netlab_ipspoof
          check: typecheck

  netlab-ipspoof-test:
    name: test (netlab-ipspoof)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ipspoof')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ipspoof
          module: netlab_ipspoof
          check: test

  netlab-icmp-lint:
    name: lint (netlab-icmp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-icmp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-icmp
          module: netlab_icmp
          check: lint

  netlab-icmp-typecheck:
    name: typecheck (netlab-icmp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-icmp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-icmp
          module: netlab_icmp
          check: typecheck

  netlab-icmp-test:
    name: test (netlab-icmp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-icmp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-icmp
          module: netlab_icmp
          check: test

  netlab-frag-lint:
    name: lint (netlab-frag)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-frag')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-frag
          module: netlab_frag
          check: lint

  netlab-frag-typecheck:
    name: typecheck (netlab-frag)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-frag')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-frag
          module: netlab_frag
          check: typecheck

  netlab-frag-test:
    name: test (netlab-frag)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-frag')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-frag
          module: netlab_frag
          check: test

  netlab-routing-lint:
    name: lint (netlab-routing)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-routing')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-routing
          module: netlab_routing
          check: lint

  netlab-routing-typecheck:
    name: typecheck (netlab-routing)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-routing')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-routing
          module: netlab_routing
          check: typecheck

  netlab-routing-test:
    name: test (netlab-routing)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-routing')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-routing
          module: netlab_routing
          check: test

  netlab-igmp-lint:
    name: lint (netlab-igmp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-igmp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-igmp
          module: netlab_igmp
          check: lint

  netlab-igmp-typecheck:
    name: typecheck (netlab-igmp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-igmp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-igmp
          module: netlab_igmp
          check: typecheck

  netlab-igmp-test:
    name: test (netlab-igmp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-igmp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-igmp
          module: netlab_igmp
          check: test

  netlab-bgp-lint:
    name: lint (netlab-bgp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-bgp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-bgp
          module: netlab_bgp
          check: lint

  netlab-bgp-typecheck:
    name: typecheck (netlab-bgp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-bgp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-bgp
          module: netlab_bgp
          check: typecheck

  netlab-bgp-test:
    name: test (netlab-bgp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-bgp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-bgp
          module: netlab_bgp
          check: test

  netlab-vrrp-hsrp-lint:
    name: lint (netlab-vrrp-hsrp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-vrrp-hsrp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-vrrp-hsrp
          module: netlab_vrrp_hsrp
          check: lint

  netlab-vrrp-hsrp-typecheck:
    name: typecheck (netlab-vrrp-hsrp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-vrrp-hsrp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-vrrp-hsrp
          module: netlab_vrrp_hsrp
          check: typecheck

  netlab-vrrp-hsrp-test:
    name: test (netlab-vrrp-hsrp)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-vrrp-hsrp')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-vrrp-hsrp
          module: netlab_vrrp_hsrp
          check: test

  netlab-portscan-lint:
    name: lint (netlab-portscan)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-portscan')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-portscan
          module: netlab_portscan
          check: lint

  netlab-portscan-typecheck:
    name: typecheck (netlab-portscan)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-portscan')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-portscan
          module: netlab_portscan
          check: typecheck

  netlab-portscan-test:
    name: test (netlab-portscan)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-portscan')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-portscan
          module: netlab_portscan
          check: test

  netlab-synflood-lint:
    name: lint (netlab-synflood)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-synflood')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-synflood
          module: netlab_synflood
          check: lint

  netlab-synflood-typecheck:
    name: typecheck (netlab-synflood)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-synflood')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-synflood
          module: netlab_synflood
          check: typecheck

  netlab-synflood-test:
    name: test (netlab-synflood)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-synflood')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-synflood
          module: netlab_synflood
          check: test

  netlab-tcphijack-lint:
    name: lint (netlab-tcphijack)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tcphijack')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tcphijack
          module: netlab_tcphijack
          check: lint

  netlab-tcphijack-typecheck:
    name: typecheck (netlab-tcphijack)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tcphijack')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tcphijack
          module: netlab_tcphijack
          check: typecheck

  netlab-tcphijack-test:
    name: test (netlab-tcphijack)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-tcphijack')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-tcphijack
          module: netlab_tcphijack
          check: test

  netlab-amplif-lint:
    name: lint (netlab-amplif)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-amplif')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-amplif
          module: netlab_amplif
          check: lint

  netlab-amplif-typecheck:
    name: typecheck (netlab-amplif)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-amplif')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-amplif
          module: netlab_amplif
          check: typecheck

  netlab-amplif-test:
    name: test (netlab-amplif)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-amplif')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-amplif
          module: netlab_amplif
          check: test

  netlab-ids-evasion-lint:
    name: lint (netlab-ids-evasion)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ids-evasion')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ids-evasion
          module: netlab_ids_evasion
          check: lint

  netlab-ids-evasion-typecheck:
    name: typecheck (netlab-ids-evasion)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ids-evasion')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ids-evasion
          module: netlab_ids_evasion
          check: typecheck

  netlab-ids-evasion-test:
    name: test (netlab-ids-evasion)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-ids-evasion')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-ids-evasion
          module: netlab_ids_evasion
          check: test

  netlab-covert-lint:
    name: lint (netlab-covert)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-covert')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-covert
          module: netlab_covert
          check: lint

  netlab-covert-typecheck:
    name: typecheck (netlab-covert)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-covert')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-covert
          module: netlab_covert
          check: typecheck

  netlab-covert-test:
    name: test (netlab-covert)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-covert')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-covert
          module: netlab_covert
          check: test

  netlab-dns-lint:
    name: lint (netlab-dns)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dns')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dns
          module: netlab_dns
          check: lint

  netlab-dns-typecheck:
    name: typecheck (netlab-dns)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dns')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dns
          module: netlab_dns
          check: typecheck

  netlab-dns-test:
    name: test (netlab-dns)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dns')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dns
          module: netlab_dns
          check: test

  netlab-dnstunnel-lint:
    name: lint (netlab-dnstunnel)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dnstunnel')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dnstunnel
          module: netlab_dnstunnel
          check: lint

  netlab-dnstunnel-typecheck:
    name: typecheck (netlab-dnstunnel)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dnstunnel')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dnstunnel
          module: netlab_dnstunnel
          check: typecheck

  netlab-dnstunnel-test:
    name: test (netlab-dnstunnel)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-dnstunnel')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-dnstunnel
          module: netlab_dnstunnel
          check: test

  netlab-icmptunnel-lint:
    name: lint (netlab-icmptunnel)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-icmptunnel')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-icmptunnel
          module: netlab_icmptunnel
          check: lint

  netlab-icmptunnel-typecheck:
    name: typecheck (netlab-icmptunnel)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-icmptunnel')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-icmptunnel
          module: netlab_icmptunnel
          check: typecheck

  netlab-icmptunnel-test:
    name: test (netlab-icmptunnel)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-icmptunnel')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-icmptunnel
          module: netlab_icmptunnel
          check: test

  release-bump:
    name: release-bump
    needs:
      - detect
      - headers
      - netlab-core-lint
      - netlab-core-typecheck
      - netlab-core-test
      - netlab-wifi-recon-lint
      - netlab-wifi-recon-typecheck
      - netlab-wifi-recon-test
      - netlab-wifi-deauth-lint
      - netlab-wifi-deauth-typecheck
      - netlab-wifi-deauth-test
      - netlab-evil-twin-lint
      - netlab-evil-twin-typecheck
      - netlab-evil-twin-test
      - netlab-wpa-crack-lint
      - netlab-wpa-crack-typecheck
      - netlab-wpa-crack-test
      - netlab-wps-brute-lint
      - netlab-wps-brute-typecheck
      - netlab-wps-brute-test
      - netlab-tap-lint
      - netlab-tap-typecheck
      - netlab-tap-test
      - netlab-tempest-lint
      - netlab-tempest-typecheck
      - netlab-tempest-test
      - netlab-hw-implant-lint
      - netlab-hw-implant-typecheck
      - netlab-hw-implant-test
      - netlab-dhcp-lint
      - netlab-dhcp-typecheck
      - netlab-dhcp-test
      - netlab-arp-lint
      - netlab-arp-typecheck
      - netlab-arp-test
      - netlab-macflood-lint
      - netlab-macflood-typecheck
      - netlab-macflood-test
      - netlab-stp-lint
      - netlab-stp-typecheck
      - netlab-stp-test
      - netlab-vlan-lint
      - netlab-vlan-typecheck
      - netlab-vlan-test
      - netlab-ipv6-ra-lint
      - netlab-ipv6-ra-typecheck
      - netlab-ipv6-ra-test
      - netlab-discovery-lint
      - netlab-discovery-typecheck
      - netlab-discovery-test
      - netlab-lldp-med-lint
      - netlab-lldp-med-typecheck
      - netlab-lldp-med-test
      - netlab-8021x-lint
      - netlab-8021x-typecheck
      - netlab-8021x-test
      - netlab-macsec-lint
      - netlab-macsec-typecheck
      - netlab-macsec-test
      - netlab-ipspoof-lint
      - netlab-ipspoof-typecheck
      - netlab-ipspoof-test
      - netlab-icmp-lint
      - netlab-icmp-typecheck
      - netlab-icmp-test
      - netlab-frag-lint
      - netlab-frag-typecheck
      - netlab-frag-test
      - netlab-routing-lint
      - netlab-routing-typecheck
      - netlab-routing-test
      - netlab-igmp-lint
      - netlab-igmp-typecheck
      - netlab-igmp-test
      - netlab-bgp-lint
      - netlab-bgp-typecheck
      - netlab-bgp-test
      - netlab-vrrp-hsrp-lint
      - netlab-vrrp-hsrp-typecheck
      - netlab-vrrp-hsrp-test
      - netlab-portscan-lint
      - netlab-portscan-typecheck
      - netlab-portscan-test
      - netlab-synflood-lint
      - netlab-synflood-typecheck
      - netlab-synflood-test
      - netlab-tcphijack-lint
      - netlab-tcphijack-typecheck
      - netlab-tcphijack-test
      - netlab-amplif-lint
      - netlab-amplif-typecheck
      - netlab-amplif-test
      - netlab-ids-evasion-lint
      - netlab-ids-evasion-typecheck
      - netlab-ids-evasion-test
      - netlab-covert-lint
      - netlab-covert-typecheck
      - netlab-covert-test
      - netlab-dns-lint
      - netlab-dns-typecheck
      - netlab-dns-test
      - netlab-dnstunnel-lint
      - netlab-dnstunnel-typecheck
      - netlab-dnstunnel-test
      - netlab-icmptunnel-lint
      - netlab-icmptunnel-typecheck
      - netlab-icmptunnel-test
    if: >-
      github.event_name == 'push' && github.ref == 'refs/heads/main'
      && !failure() && !cancelled()
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      bumped: ${{ steps.bump.outputs.bumped }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: true
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Configure git author
        run: |
          NAME="${{ github.event.head_commit.author.name }}"
          EMAIL="${{ github.event.head_commit.author.email }}"
          git config user.name "${NAME:-github-actions[bot]}"
          git config user.email "${EMAIL:-41898282+github-actions[bot]@users.noreply.github.com}"
      - name: Detect GPG availability
        id: gpg
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run: |
          if [ -n "$GPG_PRIVATE_KEY" ] && [ -n "$GPG_PASSPHRASE" ]; then
            echo "$GPG_PRIVATE_KEY" | gpg --batch --import
            echo test | gpg --batch --yes --passphrase "$GPG_PASSPHRASE" --pinentry-mode loopback --sign > /dev/null
            KEY_ID=$(gpg --list-secret-keys --with-colons | awk -F: '/^sec/{print $5; exit}')
            git config user.signingkey "$KEY_ID"; git config commit.gpgsign true
            echo "sign=--sign" >> "$GITHUB_OUTPUT"
          else
            echo "sign=" >> "$GITHUB_OUTPUT"
          fi
      - name: Plan
        run: uv tool run multicz plan --summary "$GITHUB_STEP_SUMMARY"
      - id: bump
        name: Bump, tag, push
        run: |
          set -euo pipefail
          PLAN=$(uv tool run multicz bump --commit --tag --push ${{ steps.gpg.outputs.sign }} --output json)
          echo "bumped=$(echo "$PLAN" | jq -c '.bumps | keys')" >> "$GITHUB_OUTPUT"

  release-netlab-core:
    name: release (netlab-core)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-core')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-core
        run: uv build --package netlab-core --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-core)
          TAG="netlab-core-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-wifi-recon:
    name: release (netlab-wifi-recon)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-wifi-recon
        run: uv build --package netlab-wifi-recon --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-wifi-recon)
          TAG="netlab-wifi-recon-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-wifi-deauth:
    name: release (netlab-wifi-deauth)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-wifi-deauth')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-wifi-deauth
        run: uv build --package netlab-wifi-deauth --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-wifi-deauth)
          TAG="netlab-wifi-deauth-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-evil-twin:
    name: release (netlab-evil-twin)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-evil-twin')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-evil-twin
        run: uv build --package netlab-evil-twin --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-evil-twin)
          TAG="netlab-evil-twin-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-wpa-crack:
    name: release (netlab-wpa-crack)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-wpa-crack')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-wpa-crack
        run: uv build --package netlab-wpa-crack --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-wpa-crack)
          TAG="netlab-wpa-crack-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-wps-brute:
    name: release (netlab-wps-brute)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-wps-brute')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-wps-brute
        run: uv build --package netlab-wps-brute --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-wps-brute)
          TAG="netlab-wps-brute-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-tap:
    name: release (netlab-tap)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-tap')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-tap
        run: uv build --package netlab-tap --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-tap)
          TAG="netlab-tap-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-tempest:
    name: release (netlab-tempest)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-tempest')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-tempest
        run: uv build --package netlab-tempest --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-tempest)
          TAG="netlab-tempest-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-hw-implant:
    name: release (netlab-hw-implant)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-hw-implant')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-hw-implant
        run: uv build --package netlab-hw-implant --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-hw-implant)
          TAG="netlab-hw-implant-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-dhcp:
    name: release (netlab-dhcp)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-dhcp')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-dhcp
        run: uv build --package netlab-dhcp --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-dhcp)
          TAG="netlab-dhcp-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-arp:
    name: release (netlab-arp)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-arp')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-arp
        run: uv build --package netlab-arp --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-arp)
          TAG="netlab-arp-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-macflood:
    name: release (netlab-macflood)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-macflood')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-macflood
        run: uv build --package netlab-macflood --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-macflood)
          TAG="netlab-macflood-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-stp:
    name: release (netlab-stp)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-stp')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-stp
        run: uv build --package netlab-stp --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-stp)
          TAG="netlab-stp-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-vlan:
    name: release (netlab-vlan)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-vlan')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-vlan
        run: uv build --package netlab-vlan --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-vlan)
          TAG="netlab-vlan-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-ipv6-ra:
    name: release (netlab-ipv6-ra)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-ipv6-ra')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-ipv6-ra
        run: uv build --package netlab-ipv6-ra --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-ipv6-ra)
          TAG="netlab-ipv6-ra-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-discovery:
    name: release (netlab-discovery)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-discovery')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-discovery
        run: uv build --package netlab-discovery --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-discovery)
          TAG="netlab-discovery-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-lldp-med:
    name: release (netlab-lldp-med)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-lldp-med')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-lldp-med
        run: uv build --package netlab-lldp-med --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-lldp-med)
          TAG="netlab-lldp-med-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-8021x:
    name: release (netlab-8021x)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-8021x')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-8021x
        run: uv build --package netlab-8021x --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-8021x)
          TAG="netlab-8021x-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-macsec:
    name: release (netlab-macsec)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-macsec')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-macsec
        run: uv build --package netlab-macsec --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-macsec)
          TAG="netlab-macsec-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-ipspoof:
    name: release (netlab-ipspoof)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-ipspoof')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-ipspoof
        run: uv build --package netlab-ipspoof --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-ipspoof)
          TAG="netlab-ipspoof-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-icmp:
    name: release (netlab-icmp)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-icmp')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-icmp
        run: uv build --package netlab-icmp --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-icmp)
          TAG="netlab-icmp-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-frag:
    name: release (netlab-frag)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-frag')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-frag
        run: uv build --package netlab-frag --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-frag)
          TAG="netlab-frag-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-routing:
    name: release (netlab-routing)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-routing')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-routing
        run: uv build --package netlab-routing --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-routing)
          TAG="netlab-routing-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-igmp:
    name: release (netlab-igmp)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-igmp')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-igmp
        run: uv build --package netlab-igmp --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-igmp)
          TAG="netlab-igmp-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-bgp:
    name: release (netlab-bgp)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-bgp')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-bgp
        run: uv build --package netlab-bgp --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-bgp)
          TAG="netlab-bgp-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-vrrp-hsrp:
    name: release (netlab-vrrp-hsrp)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-vrrp-hsrp')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-vrrp-hsrp
        run: uv build --package netlab-vrrp-hsrp --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-vrrp-hsrp)
          TAG="netlab-vrrp-hsrp-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-portscan:
    name: release (netlab-portscan)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-portscan')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-portscan
        run: uv build --package netlab-portscan --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-portscan)
          TAG="netlab-portscan-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-synflood:
    name: release (netlab-synflood)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-synflood')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-synflood
        run: uv build --package netlab-synflood --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-synflood)
          TAG="netlab-synflood-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-tcphijack:
    name: release (netlab-tcphijack)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-tcphijack')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-tcphijack
        run: uv build --package netlab-tcphijack --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-tcphijack)
          TAG="netlab-tcphijack-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-amplif:
    name: release (netlab-amplif)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-amplif')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-amplif
        run: uv build --package netlab-amplif --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-amplif)
          TAG="netlab-amplif-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-ids-evasion:
    name: release (netlab-ids-evasion)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-ids-evasion')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-ids-evasion
        run: uv build --package netlab-ids-evasion --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-ids-evasion)
          TAG="netlab-ids-evasion-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-covert:
    name: release (netlab-covert)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-covert')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-covert
        run: uv build --package netlab-covert --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-covert)
          TAG="netlab-covert-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-dns:
    name: release (netlab-dns)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-dns')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-dns
        run: uv build --package netlab-dns --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-dns)
          TAG="netlab-dns-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-dnstunnel:
    name: release (netlab-dnstunnel)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-dnstunnel')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-dnstunnel
        run: uv build --package netlab-dnstunnel --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-dnstunnel)
          TAG="netlab-dnstunnel-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-icmptunnel:
    name: release (netlab-icmptunnel)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-icmptunnel')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-icmptunnel
        run: uv build --package netlab-icmptunnel --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-icmptunnel)
          TAG="netlab-icmptunnel-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-docs:
    name: release (docs)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'docs')
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
      - uses: actions/configure-pages@v6
        with:
          enablement: true
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install Zensical (doc group)
        run: uv sync --frozen --only-group doc
      - name: Build site
        run: uv run zensical build --clean
      - uses: actions/upload-pages-artifact@v5
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v5
