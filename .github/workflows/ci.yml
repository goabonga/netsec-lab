# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Chris <goabonga@pm.me>

# GENERATED by scripts/gen_ci.py from scripts/catalogue.py - do not edit by hand.
#
# No matrix: one explicit job per package per check, each gated on whether that
# component actually changed (multicz changed). The release fan-out bumps the
# workspace and creates a GitHub release per bumped package (+ the docs site).

name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:

  detect:
    name: detect changed components
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.changed.outputs.changed }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Validate multicz configuration
        run: uv tool run multicz validate --strict
      - id: changed
        name: Compute changed components
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            CH=$(uv tool run multicz changed --since "origin/${{ github.base_ref }}" --output json | jq -c '.changed')
          else
            CH=$(uv tool run multicz changed --output json | jq -c '.changed')
          fi
          echo "changed=$CH" >> "$GITHUB_OUTPUT"
          echo "## Changed: \`$CH\`" >> "$GITHUB_STEP_SUMMARY"

  headers:
    name: license-headers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: python3 scripts/add_license_header.py --path packages --types py --check
      - run: python3 scripts/add_license_header.py --path scripts --types py --check

  netlab-core-lint:
    name: lint (netlab-core)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-core')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-core
          module: netlab_core
          check: lint

  netlab-core-typecheck:
    name: typecheck (netlab-core)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-core')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-core
          module: netlab_core
          check: typecheck

  netlab-core-test:
    name: test (netlab-core)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-core')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-core
          module: netlab_core
          check: test

  netlab-wifi-recon-lint:
    name: lint (netlab-wifi-recon)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-recon
          module: netlab_wifi_recon
          check: lint

  netlab-wifi-recon-typecheck:
    name: typecheck (netlab-wifi-recon)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-recon
          module: netlab_wifi_recon
          check: typecheck

  netlab-wifi-recon-test:
    name: test (netlab-wifi-recon)
    needs: [detect, headers]
    if: contains(fromJson(needs.detect.outputs.changed), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/python-check
        with:
          path: packages/netlab-wifi-recon
          module: netlab_wifi_recon
          check: test

  release-bump:
    name: release-bump
    needs:
      - detect
      - headers
      - netlab-core-lint
      - netlab-core-typecheck
      - netlab-core-test
      - netlab-wifi-recon-lint
      - netlab-wifi-recon-typecheck
      - netlab-wifi-recon-test
    if: >-
      github.event_name == 'push' && github.ref == 'refs/heads/main'
      && !failure() && !cancelled()
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      bumped: ${{ steps.bump.outputs.bumped }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: true
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Configure git author
        run: |
          NAME="${{ github.event.head_commit.author.name }}"
          EMAIL="${{ github.event.head_commit.author.email }}"
          git config user.name "${NAME:-github-actions[bot]}"
          git config user.email "${EMAIL:-41898282+github-actions[bot]@users.noreply.github.com}"
      - name: Detect GPG availability
        id: gpg
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run: |
          if [ -n "$GPG_PRIVATE_KEY" ] && [ -n "$GPG_PASSPHRASE" ]; then
            echo "$GPG_PRIVATE_KEY" | gpg --batch --import
            echo test | gpg --batch --yes --passphrase "$GPG_PASSPHRASE" --pinentry-mode loopback --sign > /dev/null
            KEY_ID=$(gpg --list-secret-keys --with-colons | awk -F: '/^sec/{print $5; exit}')
            git config user.signingkey "$KEY_ID"; git config commit.gpgsign true
            echo "sign=--sign" >> "$GITHUB_OUTPUT"
          else
            echo "sign=" >> "$GITHUB_OUTPUT"
          fi
      - name: Plan
        run: uv tool run multicz plan --summary "$GITHUB_STEP_SUMMARY"
      - id: bump
        name: Bump, tag, push
        run: |
          set -euo pipefail
          PLAN=$(uv tool run multicz bump --commit --tag --push ${{ steps.gpg.outputs.sign }} --output json)
          echo "bumped=$(echo "$PLAN" | jq -c '.bumps | keys')" >> "$GITHUB_OUTPUT"

  release-netlab-core:
    name: release (netlab-core)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-core')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-core
        run: uv build --package netlab-core --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-core)
          TAG="netlab-core-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-netlab-wifi-recon:
    name: release (netlab-wifi-recon)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'netlab-wifi-recon')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install multicz
        run: uv tool install multicz
      - name: Build netlab-wifi-recon
        run: uv build --package netlab-wifi-recon --out-dir dist
      - name: GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          V=$(uv tool run multicz get netlab-wifi-recon)
          TAG="netlab-wifi-recon-v$V"
          uv tool run multicz release-notes --tag "$TAG" \
            | gh release create "$TAG" --title "$TAG" --notes-file - dist/*

  release-docs:
    name: release (docs)
    needs: [release-bump]
    if: >-
      !failure() && !cancelled()
      && contains(fromJson(needs.release-bump.outputs.bumped), 'docs')
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
      - uses: actions/configure-pages@v6
        with:
          enablement: true
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ github.job }}
      - name: Install Zensical (doc group)
        run: uv sync --frozen --only-group doc
      - name: Build site
        run: uv run zensical build --clean
      - uses: actions/upload-pages-artifact@v5
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v5
